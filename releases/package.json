{
  "type": "module",
  "dependencies": {
    "openapi-merge-cli": "^1.3.1"
  },
  "scripts": {
    "build": "node main.js && openapi-merge-cli --config ./build/openapi-merge.json",
    "local": "openapi-merge-cli --config ./versions/local/openapi-merge.json"
  }
}
