type: 'tool'
language: 'go'

dependsOn:
    - 'sdk'

workspace:
    inheritedTasks:
        exclude: ['lint', 'test']

tasks:
    tests:
        command: 'task tests'
        platform: 'system'
        options:
            runInCI: false
        deps:
            - 'sdk:build'
